<%= form_with(model: dealership, local: true) do |form| %>
  <% if dealership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dealership.errors.count, "error") %> prohibited this dealership from being saved:</h2>

      <ul>
      <% dealership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :dealership_name %>
  </div>

  <div class="field">
    <% Vehicle.all.each do |vehicle| %>
      <%= check_box_tag "dealership[vehicle_ids][]", vehicle.id, @dealership.vehicle_ids.include?(vehicle.id) %>
      <%= vehicle.display_name %>
      <br />
    <% end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
